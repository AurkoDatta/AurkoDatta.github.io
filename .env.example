VITE_GITHUB_USERNAME=AurkoDatta

# Optional: raises the build-time GitHub fetch rate limit from 60/hr to 5000/hr.
# Never commit a real token. Used only by scripts/fetch-repos.mjs at build time.
GITHUB_TOKEN=
